Pytest Fixtures: Fixtures are the functions that will be executed prior to each test method to which they are applied. 
Therefore, we define fixtures rather than using the same code in every test. Fixtures are used to provide data to the tests such as database connections, URLs to test and some sort of input data.


PART 3 — SET UP GITHUB ACTIONS CI/CD PIPELINE
-----------------------------------------------
Create a workflow file at:
   .github/workflows/ci.yml
 
The workflow must:
   
Trigger on every push and pull request to the main branch
 
Contain two jobs as described below

 JOB 1 — TEST
 
Runs on ubuntu-latest
Sets up Python
Installs dependencies
Runs pytest

 JOB 2 — BUILD
 
Runs only if the Test job passes (use the "needs" keyword)
Builds the Docker image using the Dockerfile
Confirms the build was successful
No push to a registry is required

























Ignore


name: Set up CI/CD Pipeline

on:
  push:
    branches:
        - main
  pull_request:
    branches:
        - main

jobs:
    test:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python-version: ["pypy3.10", "3.9", "3.10", "3.11", "3.12", "3.13"]

        steps:
            - uses: actions/checkout@v5
            - name: Set up Python ${{ matrix.python-version }}
              uses: actions/setup-python@v5
              with:
                python-version: ${{ matrix.python-version }}
            - name: Install dependencies
              run: |
                python -m pip install --upgrade pip setuptools wheel
                pip install -r requirements.txt
            - name: Run tests with pytest
              run: pytest ./test_app.py

    build:
        runs-on: ubuntu-latest
        needs: test
        name: Build Docker image (no push to registry)
        steps:
            # - name: Docker Authenticate
            #   id: docker-auth
            #   env:
            #     DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
            #     DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
            #   run: |
            #     docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_TOKEN

            - uses: actions/checkout@v5
            - name: Build Docker image
              run: docker build -t my-docker-app .
            - name: Verification of image being built
              run: docker images my-docker-app
        

